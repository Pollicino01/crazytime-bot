services:
  - type: web
    name: crazy-time-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    plan: free
    envVars:
      - key: TELEGRAM_TOKEN
        sync: false
      - key: CHANNEL_ID
        sync: false
      - key: PORT
        value: "10000"
    healthCheckPath: /healthz
    autoDeploy: true
